<!-- <!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <title></title>
  <meta name="description" content="" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="stylesheet" href="" />
  <script src="../plotly.min.js"></script>
</head>

<body>
  DASHBOARD
  <script src="../controllers/vis_res.js" type="text/javascript"></script>
  <script src="../controllers/make_chart.js" type="text/javascript"></script>
  <script>
    chartResults.forEach(function (element) {
      // Generate div tag with id = ID
      var div = document.createElement('div');
      var id = element.ID;
      div.id = id;

      // Append the div to the document body or any other container element
      document.body.appendChild(div);

      // Plot the chart using Plotly
      Plotly.newPlot(id, element.traces, element.layout, CONFIG);
    });
  </script>
</body>

</html> -->

<!-- <!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <title></title>
  <meta name="description" content="" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="stylesheet" href="" /> -->
<!-- <script src="../plotly/lib/index.js" ></script> -->
<!-- <script src="https://cdn.plot.ly/plotly-2.24.1.min.js" charset="utf-8"></script>
</head>

<body>
  DASHBOARD
  <script>

    // const chartResults = '<%- JSON.stringify(chartResults) %>';

    let chartResults = `<%- JSON.stringify(chartResults, (key, value) => {
  return (typeof value === 'string') ? value.replace('\"', '') : value;
}) %>`;

    console.log("Chartresult :-", chartResults);

    const finalresult = JSON.parse(chartResults);

    console.log("finalresult :-", finalresult);

    finalresult.forEach((element, index) => {
      console.log("element:", element);
      const div = document.createElement('div');
      console.log("div tag :-", div);
      div.id = `div-${index}`; // Assigning an ID to the div tag
      console.log("id tag :- ", div.id);
      document.body.appendChild(div);
      Plotly.newPlot(div.id, element);
    })
  </script>
</body>

</html> -->


<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <title></title>
  <meta name="description" content="" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="stylesheet" href="" />

  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 20px;
    }

    .chart-container {
      margin-bottom: 20px;
    }
  </style>

  <script src="https://cdn.plot.ly/plotly-2.24.1.min.js" charset="utf-8"></script>
</head>

<body>
  <h1>DASHBOARD</h1>

  <script>
    let chartResults = `<%- JSON.stringify(chartResults, (key, value) => {
  return (typeof value === 'string') ? value.replace('\"', '') : value;
}) %>`;

    console.log("Chartresult:", chartResults);
    const finalresult = JSON.parse(chartResults);
    console.log("finalresult:", finalresult);

    finalresult.forEach((element, index) => {
      console.log("element:", element);
      const div = document.createElement('div');
      div.className = 'chart-container';
      div.id = `div-${index}`;
      document.body.appendChild(div);
      Plotly.newPlot(div.id, element);
    });
  </script>
</body>

</html>


<!-- <% chartResults.forEach(function(chart) { %>
  <div class="chart-container" style="width: <%= (chart.columns / 12) * 100 %>%;">
    <div id="<%= chart.visualizationId %>"></div>
  </div>
  <% }); %>

    <script>
      const chartResults = <% - JSON.stringify(chartResults) %>;

      chartResults.forEach((chart) => {
        const div = document.createElement('div');
        div.className = 'chart-container';
        div.style.width = `${(chart.columns / 12) * 100}%`;
        div.id = chart.visualizationId;
        document.body.appendChild(div);
        Plotly.newPlot(div.id, chart.chartResult);
      });
    </script> -->